% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retry_utils.R
\name{send_to_llm_with_retry}
\alias{send_to_llm_with_retry}
\title{Send prompt to LLM with retry logic}
\usage{
send_to_llm_with_retry(
  connection,
  prompt,
  max_retries = 3,
  base_delay = 1,
  max_delay = 60,
  retry_on_errors = c("rate limit", "timeout", "connection", "503", "502", "429", "500"),
  ...
)
}
\arguments{
\item{connection}{LLM connection object}

\item{prompt}{The prompt to send}

\item{max_retries}{Maximum number of retry attempts (default: 3)}

\item{base_delay}{Base delay in seconds for exponential backoff (default: 1)}

\item{max_delay}{Maximum delay between retries in seconds (default: 60)}

\item{retry_on_errors}{Character vector of error patterns that should trigger retry}

\item{...}{Additional arguments passed to send_to_llm}
}
\value{
The LLM response text
}
\description{
Sends a prompt to an LLM with automatic retry on failure, exponential backoff,
and proper error handling for different types of failures.
}
\examples{
\dontrun{
conn <- create_llm_connection("gemini", "your_api_key", "gemini-1.0-pro")
response <- send_to_llm_with_retry(
  conn,
  "Explain linear regression",
  max_retries = 3
)
}
}
